import React from "react";
import ReactDOM from "react-dom/client";
import FirmwareTestPanel from "./FirmwareTestPanel.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FirmwareTestPanel />
  </React.StrictMode>,
);
